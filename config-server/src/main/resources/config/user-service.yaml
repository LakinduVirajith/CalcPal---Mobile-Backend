eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  tracing:
    sampling:
      probability: 1.0

#JWT
application:
  security:
    jwt:
      secret-key: 88d0d6a25931c6e207a09c9ac23f3469db33f07be1d425c7bb0124c22d9efeae
      expiration: 7200000 #TWO HOURS
      refresh-token:
        expiration: 2419200000 #ONE MONTH

#JAVA MAIL
spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: rp.calcpal@gmail.com
    password: ykug sgmx nnxn jnjh
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

#BASE URL
base:
  url: http://localhost:8081

---
#DEV PROFILE
server:
  port: 8081

spring:
  config:
    activate:
      on-profile: dev
  data:
    mongodb:
      uri: mongodb+srv://lakindu:calc-lakindu@calcpal-cluster.8adffay.mongodb.net/
      database: CalcPal-Dev-DB

---
#QA PROFILE
server:
  port: 8081

spring:
  config:
    activate:
      on-profile: qa
  data:
    mongodb:
      uri: mongodb+srv://lakindu:calc-lakindu@calcpal-cluster.8adffay.mongodb.net/
      database: CalcPal-Qa-DB

---
#PROD PROFILE
server:
  port: 8081

spring:
  config:
    activate:
      on-profile: prod
  data:
    mongodb:
      uri: mongodb+srv://lakindu:calc-lakindu@calcpal-cluster.8adffay.mongodb.net/
      database: CalcPal-Prod-DB
