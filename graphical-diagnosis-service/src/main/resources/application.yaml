spring:
  application:
    name: graphical-diagnosis-service
  profiles:
    active: dev
  config:
    import: "optional:configserver:http://config-server:8070"

#JAVA MAIL
mail:
  username: ${MAIL_ADDRESS}
  password: ${MAIL_APP_PASSWORD}
